defaults: &defaults
   adapter: mysql2
   username: root
   password: 
   host: localhost

development:
   <<: *defaults
   database: newtech_development

test:
   <<: *defaults
   database: newtech_test

