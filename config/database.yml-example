defaults: &defaults
   adapter: postgresql
   username: root
   password: 
   host: localhost

development:
   <<: *defaults
   database: newtech_development

test:
   <<: *defaults
   database: newtech_test

