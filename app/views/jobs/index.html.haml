%table.table.table-striped.table-bordered.table-condensed
  %thead
    %th Title
    %th Company
    %th Expires On
    %th.cred Actions

  - if @jobs.empty?
    %tr
      %td(colspan="4") Looks like none of your companies have any job postings yet. Why not create one?
  - else
    - @jobs.each do |job|
      %tr
        %td= job.title
        %td= job.company_name
        %td= job.expires_date
        %td.actions
          = link_to content_tag(:i, nil, :class => 'icon-edit icon-large'), [ :edit, job ], id: "edit_#{job.id}", class: "btn small"
          = link_to content_tag(:i, nil, :class => 'icon-trash icon-large'), job, method: :delete, id: "edit_#{job.id}", class: "btn small", :confirm => 'are you sure?'

= link_to "Add a new job posting", new_job_path, class: 'btn btn-important'

= paginate @jobs
