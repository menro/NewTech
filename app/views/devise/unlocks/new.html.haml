.span8.pull-left
  = simple_form_for(resource, :validate => true, :as => resource_name, :url => unlock_path(resource_name), :html => {:class => 'well form form-horizontal registration-box' }) do |f|
    %h2 Resend unlock instructions
    .inputs
      = f.text_field :email, :class => "text top", :placeholder => "Email"
    .form-actions
      = f.submit "Resend unlock instructions", :class => "btn btn-success"
      = link_to "Forgot your password?", new_password_path(resource_name), :class => "btn"
      %hr
      = link_to "Didn't receive confirmation instructions?", new_confirmation_path(resource_name)
      = link_to "Login", new_session_path(resource_name)

.offset8.registration-slogan


